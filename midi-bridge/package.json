{
  "name": "vibeyond-midi-bridge",
  "version": "1.0.0",
  "type": "module",
  "description": "MIDI bridge server for Vibeyond — reads USB MIDI, serves the PWA, and forwards note events over WebSocket.",
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc"
  },
  "dependencies": {
    "@inquirer/prompts": "^8.2.0",
    "@julusian/midi": "^3.1.0",
    "sirv": "^3.0.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.0",
    "typescript": "~5.6.2"
  }
}
